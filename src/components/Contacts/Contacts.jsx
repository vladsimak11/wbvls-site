import { Section, Header } from '../App.styled';

const Contacts = () => {
  return (
    <Section>
      <Header id="contacts">
        Contacts
      </Header>
    </Section>
  )
}

export default Contacts;